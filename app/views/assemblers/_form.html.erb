<%= form_for(@assembler) do |f| %>
  <% if @assembler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assembler.errors.count, "error") %> prohibited this assembler from being saved:</h2>

      <ul>
      <% @assembler.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :chapter %><br />
    <%= f.text_field :chapter %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
