<%= form_for(@brafe) do |f| %>
  <% if @brafe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brafe.errors.count, "error") %> prohibited this brafe from being saved:</h2>

      <ul>
      <% @brafe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
