<%= form_for(@hunter , :remote => true) do |f| %>
  <% if @hunter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hunter.errors.count, "error") %> prohibited this hunter from being saved:</h2>

      <ul>
      <% @hunter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :regexp %><br />
    <%= f.text_field :regexp %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
