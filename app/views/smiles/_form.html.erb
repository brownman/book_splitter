<%= form_for(@smile , :remote => true) do |f| %>
  <% if @smile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smile.errors.count, "error") %> prohibited this smile from being saved:</h2>

      <ul>
      <% @smile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

  <p>
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
